{
  "name": "heb-sdk-unofficial",
  "private": true,
  "description": "Unofficial H-E-B SDK monorepo",
  "type": "module",
  "scripts": {
    "lint": "pnpm -r lint --if-present",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "rm -rf packages/*/dist",
    "sync:extension-config": "node packages/cookie-bridge-extension/scripts/sync-env.mjs",
    "summarize-flows": "tsx scripts/summarize-flows.ts"
  },
  "packageManager": "pnpm@10.15.1",
  "devDependencies": {
    "@clerk/clerk-js": "^5.119.1",
    "@tailwindcss/cli": "^4.1.18",
    "dotenv": "^17.2.3",
    "tailwindcss": "^4.1.18",
    "tsx": "^4.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
